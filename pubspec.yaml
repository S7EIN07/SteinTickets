name: stein_tickets
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.9.0
  provider: ^6.1.2
  image_picker: ^1.2.0
  mask_text_input_formatter: ^2.9.0
  rive: ^0.13.20
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.3.11
  

flutter_launcher_icons:
  android: true # Gera ícones para Android
  ios: true     # Gera ícones para iOS
  image_path: assets/images/LogoTickets.png


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/LogoTickets.png

